# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  api_namespace: one
  properties: {
                "can_edit": true,
                "can_se": true,
                "can_share": true,
                "something_else": true
              }

two:
  api_namespace: two
  properties: {
                "can_edit": true,
                "can_se": true,
                "can_share": true,
                "something_else": true
              }

user:
  api_namespace: users
  properties: {first_name: "Don", last_name: "Restarone"}

plugin_subdomain_message_event:
  api_namespace: plugin_subdomain_events
  properties:  {
                  "model": {
                    "record_id": 1,
                    "record_type": "Message"
                  },
                  "representation": {
                    "body": "<h1>Hello</h1>"
                  }
                }

resource_with_all_types_one:
  api_namespace: namespace_with_all_types                
  properties: {
    'array': 'no',
    'object': {'a': 'apple'},
    'string': 'string 1',
    'number': 555,
    'null': true
  }
resource_with_all_types_two:
  api_namespace: namespace_with_all_types                
  properties: {
    'array': 'yes',
    'object': {'b': 'ball'},
    'string': 'string 2',
    'number': 666,
    'null': false
  }

transcript:
  api_namespace: namespace_with_transcript
  properties: {
    "raw_transcript": "1\n00:00:00,300 --> 00:00:02,167\n[sally]: i'm in los angeles and\n\n2\n00:00:01,920 --> 00:00:02,102\n[bobby]: okay\n\n3\n00:00:03,151 --> 00:00:04,737\n[sally]: maybe possibly\n\n4\n00:00:05,780 --> 00:00:13,613\n[bobby78]: $ oh maybe ye so nice an is a\nsupporter financial supporter of hollows\n\n5\n00:00:13,345 --> 00:00:15,510\n[sally]: 10 s yeah",
    "transcript": "",
    "transcript_parsed": false
  }

vacuum_job_target_resource:
  api_namespace: vacuum_job_target
  properties: {
                "can_edit": true,
                "can_se": true,
                "can_share": true,
                "something_else": true
              }

# Specific ApiResources' fixtures related to plugins are moved to its separate files.
<%= IO.read(Rails.root.join "test/plugin_fixtures/dependencies/api_resources/bishop_monitoring.yml") %>
<%= IO.read(Rails.root.join "test/plugin_fixtures/dependencies/api_resources/bishop_tls_monitoring.yml") %>
<%= IO.read(Rails.root.join "test/plugin_fixtures/dependencies/api_resources/compliance_visitor_data_request.yml") %>
<%= IO.read(Rails.root.join "test/plugin_fixtures/dependencies/api_resources/consultant.yml") %>
<%= IO.read(Rails.root.join "test/plugin_fixtures/dependencies/api_resources/mailchimp.yml") %>

# ERB is used to evaluate the embedded ruby code in time_tracker.yml file
<%= ERB.new(IO.read(Rails.root.join "test/plugin_fixtures/dependencies/api_resources/time_tracker.yml")).result %>
<%= ERB.new(IO.read(Rails.root.join "test/plugin_fixtures/dependencies/api_resources/timesheet_request.yml")).result %>
